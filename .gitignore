# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Dependencies
node_modules

# Expo
.expo
dist
web-build
expo-env.d.ts

# Native projects — generated by `expo prebuild`, never edited by hand.
# The managed workflow regenerates them from app.config.ts on every build,
# so a committed copy is a second source of truth that silently goes stale.
/android
/ios

# Metro
.metro-health-check*

# Native signing material — never committed
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# TypeScript
*.tsbuildinfo

# Env
.env*
!.env.example

# Editor directories and files
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Coverage
coverage

# AI files
.claude/settings.local.json
