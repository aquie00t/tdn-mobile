engine-strict=true

# React Native resolves native modules by walking node_modules, which pnpm's
# symlinked layout hides from it. Expo requires a hoisted store; without this
# the JS builds and the native autolinking finds nothing.
node-linker=hoisted
